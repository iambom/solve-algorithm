// 문자열을 정수로 바꾸기
// 예를들어 str이 "1234"이면 1234를 반환하고, "-1234"이면 -1234를 반환하면 됩니다. str은 부호(+,-)와 숫자로만 구성되어 있고, 잘못된 값이 입력되는 경우는 없습니다.

function solution(s) {
    var answer = parseInt(s);
    
    return answer;
}

// 다른 사람 풀이
// 문자열과 숫자의 사칙연산은 숫자가 나온다...

function strToInt(str){
    return str/1
}

function strToInt(str){
    return  +str;
}